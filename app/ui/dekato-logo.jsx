import { oswald } from "@/style/font";

const Logo = () => {
  return (
    <p className={`text-xl font-semibold uppercase ${oswald.className}`}>
      dekato-outfit
    </p>
  );
};

export default Logo;
