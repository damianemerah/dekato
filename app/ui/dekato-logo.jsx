import { oswald } from "@/style/font";

const Logo = () => {
  return (
    <div className={`${oswald.className} text-xl font-semibold uppercase`}>
      {/* dekato-outfit */}
      dekato
    </div>
  );
};

export default Logo;
