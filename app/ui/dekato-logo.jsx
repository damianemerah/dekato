import { oswald } from "@/style/font";

const Logo = () => {
  return (
    <p className={` ${oswald.className} text-xl font-semibold uppercase`}>
      dekato-outfit
    </p>
  );
};

export default Logo;
